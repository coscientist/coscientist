{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bunx turbo run build --filter=@coscientist/web",
    "watchPatterns": ["/apps/web/**"]
  },
  "deploy": {
    "startCommand": "bun run --filter=@coscientist/web start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
